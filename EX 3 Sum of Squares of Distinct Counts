def sumCounts(nums):
    n, ans = len(nums), 0
    for i in range(n):
        s = set()
        for j in range(i, n):
            s.add(nums[j])
            ans += len(s) ** 2
    return ans

nums = [1,2,1]
print(sumCounts(nums))
